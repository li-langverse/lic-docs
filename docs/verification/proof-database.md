# Proof database — compiler release gate
**Pin:** `proof-db/baseline.json` · **Report:** `scripts/proof-db-rebuild-report.sh`
Default CI: **advisory** (`LI_PROOF_DB_STRICT=0`). Strict: `LI_PROOF_DB_STRICT=1`.
